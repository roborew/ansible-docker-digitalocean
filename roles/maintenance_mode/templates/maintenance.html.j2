<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ site_title | default('Site Under Construction') }}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            color: #333;
        }

        .container {
            text-align: center;
            background: white;
            padding: 3rem 2rem;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 90%;
        }

        .icon {
            font-size: 4rem;
            margin-bottom: 2rem;
            display: block;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            color: #2d3748;
            font-weight: 600;
        }

        .subtitle {
            font-size: 1.2rem;
            color: #718096;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        .message {
            background: #f7fafc;
            padding: 1.5rem;
            border-radius: 10px;
            margin-bottom: 2rem;
            border-left: 4px solid #667eea;
        }

        .message p {
            color: #4a5568;
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 0.5rem;
        }

        .status-badge {
            display: inline-block;
            background: #ffd700;
            color: #744210;
            padding: 0.5rem 1rem;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .contact {
            font-size: 0.9rem;
            color: #718096;
            margin-top: 2rem;
        }

        .contact a {
            color: #667eea;
            text-decoration: none;
        }

        .contact a:hover {
            text-decoration: underline;
        }

        .footer {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid #e2e8f0;
            font-size: 0.8rem;
            color: #a0aec0;
        }

        @media (max-width: 768px) {
            .container {
                padding: 2rem 1.5rem;
                margin: 1rem;
            }

            h1 {
                font-size: 2rem;
            }

            .subtitle {
                font-size: 1rem;
            }

            .icon {
                font-size: 3rem;
            }
        }

        .pulse {
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.05);
            }
            100% {
                transform: scale(1);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="icon pulse">🚧</div>
        
        <div class="status-badge">{{ status_text | default('Maintenance in Progress') }}</div>
        
        <h1>{{ site_title | default('Site Under Construction') }}</h1>
        
        <p class="subtitle">
            {{ subtitle | default('We are currently performing scheduled maintenance to improve your experience.') }}
        </p>

        <div class="message">
            <p><strong>What\'s happening:</strong></p>
            <p>{{ maintenance_reason | default('We are updating our systems to serve you better. This includes deploying new features, security updates, and performance improvements.') }}</p>
            
            {% if estimated_completion %}
            <p><strong>Estimated completion:</strong> {{ estimated_completion }}</p>
            {% endif %}
        </div>

        {% if contact_email %}
        <div class="contact">
            <p>For urgent inquiries, please contact us at: 
                <a href="mailto:{{ contact_email }}">{{ contact_email }}</a>
            </p>
        </div>
        {% endif %}

        <div class="footer">
            <p>Thank you for your patience while we make improvements!</p>
            {% if maintenance_started %}
            <p>Maintenance started: {{ maintenance_started }}</p>
            {% endif %}
        </div>
    </div>
</body>
</html> 